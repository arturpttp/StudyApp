# Step 3 — Question Bank Seeding Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Populate the dev database with 15 authored medical-exam questions via an idempotent, extensible CSV-based seed pipeline.

**Architecture:** Prisma schema gets `externalId` on Question and `position` + unique constraint on Alternative. A seed runner walks `prisma/seed/sources/*.csv`, validates rows against a Zod schema, and upserts questions with stable alternative IDs keyed by `(questionId, position)`. Integration tests verify idempotency and AnswerHistory preservation.

**Tech Stack:** Prisma (PostgreSQL), Zod (CSV validation), csv-parse (parsing), tsx (runner), Vitest (testing)

---

## File Structure

```
packages/db/
├── prisma/
│   ├── schema.prisma              # + externalId on Question, + position on Alternative
│   ├── seed.ts                    # Entry point: connect → upsert taxonomies → walk CSVs → upsert questions
│   └── seed/
│       ├── taxonomies.ts          # SUBJECTS + INSTITUTIONS constant arrays
│       ├── csv-row.schema.ts      # Zod schema for one CSV row
│       ├── ingest.ts              # parseCsv() + upsertQuestion() logic
│       └── sources/
│           ├── README.md          # How to add a new source file
│           └── healthquest-authored.csv  # 15 starter questions
├── src/
│   └── index.ts                   # Existing — unchanged
├── __tests__/
│   ├── client.test.ts             # Existing — unchanged
│   └── seed.test.ts              # NEW — 5 integration tests
├── package.json                   # + tsx, csv-parse, zod devDependencies + seed script
└── vitest.config.ts               # Existing — unchanged
```

---

### Task 1: Schema Change

**Files:**
- Modify: `packages/db/prisma/schema.prisma`

- [ ] **Step 1: Add `externalId` to Question model**

In `packages/db/prisma/schema.prisma`, add `externalId` field to the Question model:

```prisma
model Question {
  id            String          @id @default(cuid())
  externalId    String?         @unique
  statement     String
  explanation   String?
  difficulty    Difficulty
  status        Status          @default(ACTIVE)
  year          Int?
  subject       Subject         @relation(fields: [subjectId], references: [id])
  subjectId     String
  institution   Institution     @relation(fields: [institutionId], references: [id])
  institutionId String
  alternatives  Alternative[]
  answers       AnswerHistory[]
  examQuestions ExamQuestion[]
}
```

- [ ] **Step 2: Add `position` and unique constraint to Alternative model**

```prisma
model Alternative {
  id         String          @id @default(cuid())
  text       String
  isCorrect  Boolean
  position   Int
  question   Question        @relation(fields: [questionId], references: [id])
  questionId String
  answers    AnswerHistory[]

  @@unique([questionId, position])
}
```

- [ ] **Step 3: Push schema to dev database**

Run:
```bash
cd packages/db && pnpm exec prisma db push
```

Expected: "Your database is now in sync with your Prisma schema."

- [ ] **Step 4: Regenerate Prisma client**

Run:
```bash
cd packages/db && pnpm exec prisma generate
```

Expected: "Generated Prisma Client" with no errors.

- [ ] **Step 5: Verify existing tests still pass**

Run:
```bash
cd packages/db && pnpm test
```

Expected: `client.test.ts` passes (2 tests).

- [ ] **Step 6: Commit**

```bash
git add packages/db/prisma/schema.prisma packages/db/generated/
git commit -m "feat(db): add externalId to Question and position to Alternative"
```

---

### Task 2: Add Dependencies and Wire Scripts

**Files:**
- Modify: `packages/db/package.json`

- [ ] **Step 1: Install dependencies**

Run:
```bash
pnpm --filter @healthquest/db add -D tsx csv-parse zod
```

- [ ] **Step 2: Add seed scripts to package.json**

Add to `"scripts"`:
```json
"seed": "tsx prisma/seed.ts"
```

Add top-level `"prisma"` key:
```json
"prisma": {
  "seed": "tsx prisma/seed.ts"
}
```

Final `packages/db/package.json`:
```json
{
  "name": "@healthquest/db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "test": "vitest run",
    "seed": "tsx prisma/seed.ts"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.7.0",
    "@prisma/client": "latest",
    "dotenv": "^16.0.0",
    "pg": "^8.20.0",
    "prisma": "latest"
  },
  "devDependencies": {
    "@healthquest/config": "workspace:*",
    "csv-parse": "^5.6.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0",
    "zod": "^3.24.0"
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add packages/db/package.json pnpm-lock.yaml
git commit -m "chore(db): add tsx, csv-parse, zod for seed pipeline"
```

---

### Task 3: Create Taxonomies Module

**Files:**
- Create: `packages/db/prisma/seed/taxonomies.ts`

- [ ] **Step 1: Create `packages/db/prisma/seed/taxonomies.ts`**

```ts
export const SUBJECTS = [
  "Enfermagem",
  "Nutrição",
  "Psicologia",
  "Saúde Coletiva",
  "Medicina",
] as const;

export const INSTITUTIONS = [
  "HealthQuest",
  "VUNESP",
  "FGV",
  "FCC",
  "ENARE",
  "Revalida",
] as const;

export type SubjectName = (typeof SUBJECTS)[number];
export type InstitutionName = (typeof INSTITUTIONS)[number];
```

- [ ] **Step 2: Commit**

```bash
git add packages/db/prisma/seed/taxonomies.ts
git commit -m "feat(db): add taxonomy constants for subjects and institutions"
```

---

### Task 4: Create CSV Row Schema

**Files:**
- Create: `packages/db/prisma/seed/csv-row.schema.ts`

- [ ] **Step 1: Create `packages/db/prisma/seed/csv-row.schema.ts`**

```ts
import { z } from "zod";

export const csvRowSchema = z.object({
  externalId: z.string().min(1),
  institution: z.string().min(1),
  subject: z.string().min(1),
  year: z
    .string()
    .regex(/^\d{4}$|^$/)
    .transform((v) => (v === "" ? null : Number(v))),
  difficulty: z.enum(["EASY", "MEDIUM", "HARD"]),
  statement: z.string().min(10),
  a: z.string().min(1),
  b: z.string().min(1),
  c: z.string().min(1),
  d: z.string().min(1),
  e: z.string().min(1),
  correct: z.enum(["a", "b", "c", "d", "e"]),
  explanation: z.string().min(10),
});

export type CsvRow = z.infer<typeof csvRowSchema>;
```

- [ ] **Step 2: Commit**

```bash
git add packages/db/prisma/seed/csv-row.schema.ts
git commit -m "feat(db): add Zod schema for CSV row validation"
```

---

### Task 5: Create Ingest Module

**Files:**
- Create: `packages/db/prisma/seed/ingest.ts`

- [ ] **Step 1: Create `packages/db/prisma/seed/ingest.ts`**

```ts
import { parse } from "csv-parse/sync";
import { readFileSync } from "node:fs";
import { csvRowSchema, type CsvRow } from "./csv-row.schema.js";
import type { PrismaClient } from "../../generated/prisma/client.js";

export function parseCsv(filePath: string): CsvRow[] {
  const content = readFileSync(filePath, "utf-8");
  const records: Record<string, string>[] = parse(content, {
    columns: true,
    skip_empty_lines: true,
    trim: true,
  });

  return records.map((record, index) => {
    const result = csvRowSchema.safeParse(record);
    if (!result.success) {
      const msg = result.error.issues
        .map((i) => `${i.path.join(".")}: ${i.message}`)
        .join("; ");
      throw new Error(`${filePath}:${index + 2} — ${msg}`);
    }
    return result.data;
  });
}

const POSITION_MAP: Record<string, number> = {
  a: 0,
  b: 1,
  c: 2,
  d: 3,
  e: 4,
};

export async function upsertQuestion(
  prisma: PrismaClient,
  row: CsvRow,
  subjectMap: Map<string, string>,
  institutionMap: Map<string, string>,
): Promise<void> {
  const subjectId = subjectMap.get(row.subject);
  if (!subjectId) {
    throw new Error(`Unknown subject: "${row.subject}"`);
  }
  const institutionId = institutionMap.get(row.institution);
  if (!institutionId) {
    throw new Error(`Unknown institution: "${row.institution}"`);
  }

  const alternatives = (["a", "b", "c", "d", "e"] as const).map((letter) => ({
    text: row[letter],
    isCorrect: row.correct === letter,
    position: POSITION_MAP[letter],
  }));

  const existing = await prisma.question.findUnique({
    where: { externalId: row.externalId },
  });

  if (existing) {
    await prisma.question.update({
      where: { externalId: row.externalId },
      data: {
        statement: row.statement,
        explanation: row.explanation,
        difficulty: row.difficulty,
        year: row.year,
        subjectId,
        institutionId,
      },
    });

    for (const alt of alternatives) {
      await prisma.alternative.upsert({
        where: {
          questionId_position: {
            questionId: existing.id,
            position: alt.position,
          },
        },
        create: {
          questionId: existing.id,
          text: alt.text,
          isCorrect: alt.isCorrect,
          position: alt.position,
        },
        update: {
          text: alt.text,
          isCorrect: alt.isCorrect,
        },
      });
    }
  } else {
    await prisma.question.create({
      data: {
        externalId: row.externalId,
        statement: row.statement,
        explanation: row.explanation,
        difficulty: row.difficulty,
        year: row.year,
        subjectId,
        institutionId,
        alternatives: {
          create: alternatives,
        },
      },
    });
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/db/prisma/seed/ingest.ts
git commit -m "feat(db): add CSV parser and question upsert logic"
```

---

### Task 6: Create Seed Entry Point

**Files:**
- Create: `packages/db/prisma/seed.ts`

- [ ] **Step 1: Create `packages/db/prisma/seed.ts`**

```ts
import { readdirSync } from "node:fs";
import { join, resolve } from "node:path";
import { fileURLToPath } from "node:url";
import { PrismaPg } from "@prisma/adapter-pg";
import { PrismaClient } from "../generated/prisma/client.js";
import { SUBJECTS, INSTITUTIONS } from "./seed/taxonomies.js";
import { parseCsv, upsertQuestion } from "./seed/ingest.js";

const __dirname = fileURLToPath(new URL(".", import.meta.url));

export interface SeedOptions {
  sourcesDir?: string;
}

export interface SeedResult {
  subjects: number;
  institutions: number;
  questions: number;
}

export async function seed(options: SeedOptions = {}): Promise<SeedResult> {
  const sourcesDir =
    options.sourcesDir ?? resolve(__dirname, "seed/sources");

  const adapter = new PrismaPg({
    connectionString: process.env.DATABASE_URL ?? "",
  });
  const prisma = new PrismaClient({ adapter });

  try {
    const subjectMap = new Map<string, string>();
    for (const name of SUBJECTS) {
      const record = await prisma.subject.upsert({
        where: { name },
        create: { name },
        update: {},
      });
      subjectMap.set(name, record.id);
    }

    const institutionMap = new Map<string, string>();
    for (const name of INSTITUTIONS) {
      const record = await prisma.institution.upsert({
        where: { name },
        create: { name },
        update: {},
      });
      institutionMap.set(name, record.id);
    }

    const files = readdirSync(sourcesDir)
      .filter((f) => f.endsWith(".csv"))
      .sort();

    let questionCount = 0;
    for (const file of files) {
      const rows = parseCsv(join(sourcesDir, file));
      for (const row of rows) {
        await upsertQuestion(prisma, row, subjectMap, institutionMap);
        questionCount++;
      }
    }

    const result: SeedResult = {
      subjects: subjectMap.size,
      institutions: institutionMap.size,
      questions: questionCount,
    };

    console.log("Seed complete:", result);
    return result;
  } finally {
    await prisma.$disconnect();
  }
}

const isMainModule =
  process.argv[1] &&
  resolve(process.argv[1]) === resolve(fileURLToPath(import.meta.url));

if (isMainModule) {
  seed().catch((err) => {
    console.error("Seed failed:", err);
    process.exit(1);
  });
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/db/prisma/seed.ts
git commit -m "feat(db): add seed entry point with taxonomy upsert and CSV walk"
```

---

### Task 7: Create Source CSV and README

**Files:**
- Create: `packages/db/prisma/seed/sources/README.md`
- Create: `packages/db/prisma/seed/sources/healthquest-authored.csv`

- [ ] **Step 1: Create `packages/db/prisma/seed/sources/README.md`**

```markdown
# Seed Sources

Drop CSV files in this directory to add questions to the database.

## CSV format

```
externalId,institution,subject,year,difficulty,statement,a,b,c,d,e,correct,explanation
```

- Comma-separated, UTF-8, quote strings with `"` if they contain commas
- `year`: 4-digit year or empty string (for authored content)
- `difficulty`: EASY, MEDIUM, or HARD
- `correct`: a, b, c, d, or e
- All text in pt-BR
- `externalId` must be unique across all files

## Adding a new source

1. Create a `.csv` file in this directory following the format above
2. Run `pnpm --filter @healthquest/db seed`
3. The runner processes files in alphabetical order and upserts by `externalId`
```

- [ ] **Step 2: Create `packages/db/prisma/seed/sources/healthquest-authored.csv`**

```csv
externalId,institution,subject,year,difficulty,statement,a,b,c,d,e,correct,explanation
authored-enf-001,HealthQuest,Enfermagem,,MEDIUM,"A Sistematização da Assistência de Enfermagem (SAE) é composta por etapas inter-relacionadas que organizam o cuidado. Qual alternativa apresenta corretamente as cinco etapas do Processo de Enfermagem conforme a Resolução COFEN 358/2009?","Anamnese, exame físico, prescrição, evolução e alta","Coleta de dados, diagnóstico de enfermagem, planejamento, implementação e avaliação","Triagem, medicação, curativo, orientação e encaminhamento","Admissão, evolução, prescrição, intercorrência e alta","Histórico, exame clínico, conduta, intervenção e prognóstico",b,"A Resolução COFEN 358/2009 estabelece que o Processo de Enfermagem é composto por cinco etapas: coleta de dados (histórico de enfermagem), diagnóstico de enfermagem, planejamento, implementação e avaliação. Essas etapas são inter-relacionadas e devem ser registradas formalmente."
authored-enf-002,HealthQuest,Enfermagem,,MEDIUM,"Durante a aferição da pressão arterial de um paciente adulto, o profissional de enfermagem deve observar diversos cuidados técnicos. Qual das alternativas abaixo representa a técnica correta?","Posicionar o manguito sobre a roupa do paciente para maior conforto","Inflar o manguito até 30 mmHg acima do desaparecimento do pulso radial","Utilizar manguito de tamanho único independentemente da circunferência do braço","Realizar a medida imediatamente após o paciente subir escadas","Posicionar o braço do paciente acima do nível do coração",b,"A técnica correta de aferição da PA exige que o manguito seja inflado até 30 mmHg acima do ponto em que o pulso radial desaparece (estimativa da PAS). O manguito deve ser colocado sobre a pele, com tamanho adequado à circunferência do braço, e o paciente deve estar em repouso de pelo menos 5 minutos."
authored-enf-003,HealthQuest,Enfermagem,,MEDIUM,"A administração segura de medicamentos exige atenção aos 'certos' da enfermagem. Quantos são os certos atualmente preconizados e qual alternativa lista corretamente três deles?","5 certos: paciente, medicamento, dose, via e horário","7 certos: paciente, medicamento, forma farmacêutica, dose, via, horário e registro","9 certos: paciente, medicamento, dose, via, horário, registro, orientação, forma farmacêutica e resposta","11 certos: paciente, medicamento, dose, via, horário, validade, lote, gotejamento, compatibilidade, orientação e registro","3 certos: paciente, dose e horário",c,"Atualmente, a literatura de segurança do paciente preconiza 9 certos na administração de medicamentos: paciente certo, medicamento certo, dose certa, via certa, horário certo, registro certo, orientação certa, forma farmacêutica certa e resposta certa. Esses certos visam prevenir eventos adversos relacionados à medicação."
authored-enf-004,HealthQuest,Enfermagem,,MEDIUM,"Em relação à biossegurança e ao descarte de perfurocortantes em serviços de saúde, conforme a NR-32 e a RDC ANVISA 222/2018, é correto afirmar que:","Agulhas usadas devem ser reencapadas com técnica de uma mão antes do descarte","O recipiente para perfurocortantes deve ser preenchido até no máximo 2/3 de sua capacidade","Lâminas de bisturi podem ser descartadas em lixo comum desde que embaladas em papel","O descarte de perfurocortantes é responsabilidade exclusiva do serviço de limpeza","Seringas sem agulha podem ser descartadas no lixo de resíduos comuns",b,"A RDC ANVISA 222/2018 e a NR-32 determinam que recipientes para perfurocortantes devem ser preenchidos até no máximo 2/3 (dois terços) de sua capacidade, devem estar identificados com símbolo de risco biológico e ser substituídos quando atingirem o limite. O reencape de agulhas é expressamente proibido."
authored-enf-005,HealthQuest,Enfermagem,,MEDIUM,"A Norma Regulamentadora 32 (NR-32) estabelece diretrizes de segurança e saúde no trabalho em serviços de saúde. Sobre seus requisitos para os profissionais de enfermagem, assinale a alternativa correta:","O uso de adornos como anéis e pulseiras é permitido desde que higienizados","O empregador deve fornecer EPIs, mas seu uso é facultativo ao trabalhador","É obrigatória a capacitação continuada dos trabalhadores quanto aos riscos biológicos","A vacinação dos profissionais é recomendada, porém não obrigatória pela norma","O Programa de Prevenção de Riscos Ambientais (PPRA) não se aplica a serviços de saúde",c,"A NR-32 determina que todos os trabalhadores de serviços de saúde devem receber capacitação continuada sobre riscos biológicos, medidas de controle, uso correto de EPIs e procedimentos em caso de acidente. A norma também proíbe adornos e torna obrigatória a vacinação e o fornecimento de EPIs."
authored-enf-006,HealthQuest,Enfermagem,,MEDIUM,"Um paciente com colostomia definitiva necessita de orientações de enfermagem para o autocuidado. Qual das alternativas apresenta uma orientação correta sobre os cuidados com a colostomia?","A bolsa coletora deve ser trocada apenas quando estiver completamente cheia","A pele periestoma deve ser limpa com álcool 70% a cada troca de bolsa","O recorte da placa protetora deve ter o mesmo diâmetro do estoma, sem folga","A irrigação da colostomia pode ser realizada pelo paciente após treinamento adequado","Alimentos formadores de gases não interferem no funcionamento da colostomia",d,"A irrigação da colostomia é uma técnica que pode ser ensinada ao paciente para regularizar o funcionamento intestinal. A bolsa deve ser esvaziada quando atingir 1/3 da capacidade, a pele periestoma deve ser limpa com água e sabão neutro (nunca álcool), e o recorte deve ter 2-3 mm de folga em relação ao estoma."
authored-enf-007,HealthQuest,Enfermagem,,MEDIUM,"No período pós-operatório imediato (POI), o enfermeiro deve estar atento a diversas complicações. Qual intervenção de enfermagem é prioritária nas primeiras horas após uma cirurgia abdominal sob anestesia geral?","Estimular deambulação precoce nas primeiras 2 horas","Monitorar sinais de depressão respiratória e manter vias aéreas pérvias","Oferecer dieta líquida completa para recuperação nutricional","Administrar laxante para prevenir constipação pós-operatória","Remover o curativo cirúrgico para avaliar a incisão",b,"No POI de cirurgia sob anestesia geral, a prioridade é a manutenção de vias aéreas pérvias e a monitorização de depressão respiratória, pois os efeitos residuais dos anestésicos podem comprometer a função respiratória. A deambulação precoce é estimulada, mas não nas primeiras 2 horas. A dieta é reintroduzida gradualmente após retorno do peristaltismo."
authored-enf-008,HealthQuest,Enfermagem,,MEDIUM,"Durante uma hemotransfusão, o paciente apresenta febre, calafrios, dor lombar e urina escurecida. Diante dessa situação, a conduta imediata da enfermagem deve ser:","Reduzir a velocidade de infusão e administrar antitérmico conforme prescrição","Interromper imediatamente a transfusão, manter acesso venoso com SF 0,9% e comunicar o médico","Suspender a transfusão e descartar a bolsa de sangue no expurgo","Aumentar a hidratação venosa e continuar a transfusão sob observação","Administrar adrenalina subcutânea e manter a infusão em velocidade mínima",b,"Os sinais descritos (febre, calafrios, dor lombar e urina escura) sugerem reação transfusional hemolítica. A conduta imediata é: interromper a transfusão, manter acesso venoso com SF 0,9% (não em Y com a bolsa), comunicar o médico, enviar a bolsa e amostras de sangue ao banco de sangue e monitorar sinais vitais."
authored-enf-009,HealthQuest,Enfermagem,,MEDIUM,"Em uma parada cardiorrespiratória (PCR) em adulto, a sequência correta do Suporte Básico de Vida (SBV) segundo as diretrizes da American Heart Association (AHA 2020) é:","Verificar responsividade → Chamar ajuda → A-B-C (via aérea, respiração, compressões)","Verificar responsividade → Chamar ajuda → C-A-B (compressões, via aérea, respiração)","Chamar ajuda → Desfibrilar → Iniciar compressões → Verificar ritmo","Verificar pulso por 30 segundos → Iniciar ventilação → Aplicar compressões","Posicionar a vítima → Realizar 2 ventilações de resgate → Iniciar compressões",b,"Desde 2010, a AHA preconiza a sequência C-A-B para o SBV em adultos: primeiro compressões torácicas (C), depois abertura de via aérea (A) e ventilações (B). A prioridade é iniciar compressões de alta qualidade o mais rápido possível. A verificação de pulso deve levar no máximo 10 segundos."
authored-enf-010,HealthQuest,Enfermagem,,MEDIUM,"Na avaliação e tratamento de feridas, a enfermagem utiliza diferentes tipos de curativos conforme a classificação da lesão. Sobre feridas e curativos, é correto afirmar que:","Feridas com tecido de granulação devem ser tratadas com curativo seco para acelerar a cicatrização","O desbridamento autolítico utiliza enzimas exógenas como colagenase","Feridas de espessura total com necrose úmida (esfacelo) são classificadas como estágio 2","A hidrogel é indicada para feridas com excesso de exsudato por sua alta capacidade absortiva","O ambiente úmido favorece a migração celular e o processo de cicatrização",e,"O conceito de cicatrização em meio úmido, introduzido por Winter (1962), demonstrou que feridas mantidas em ambiente úmido controlado cicatrizam mais rapidamente, pois o meio úmido favorece a migração celular, angiogênese e desbridamento autolítico. Curativos secos não são indicados para feridas com tecido de granulação."
authored-nut-001,HealthQuest,Nutrição,,MEDIUM,"Na dietoterapia do Diabetes Mellitus tipo 2 (DM2), a contagem de carboidratos é uma estratégia que permite maior flexibilidade alimentar. Sobre essa abordagem, é correto afirmar que:","A contagem considera apenas os carboidratos simples (açúcares) na soma total","A relação insulina/carboidrato é fixa em 1 unidade para cada 10g de carboidrato para todos os pacientes","Fibras alimentares devem ser subtraídas do total de carboidratos quando acima de 5g por porção","Proteínas e gorduras não influenciam a glicemia pós-prandial em nenhuma circunstância","O método é indicado exclusivamente para pacientes em uso de insulina",c,"Na contagem de carboidratos, quando a quantidade de fibras em uma porção ultrapassa 5g, subtrai-se esse valor do total de carboidratos (conceito de carboidrato líquido ou net carbs), pois fibras não são digeridas e não elevam a glicemia. A relação insulina/carboidrato é individualizada e o método pode ser usado por pacientes com ou sem insulina."
authored-nut-002,HealthQuest,Nutrição,,MEDIUM,"O Índice de Massa Corporal (IMC) é um indicador amplamente utilizado na avaliação nutricional de adultos. Considerando a classificação da Organização Mundial da Saúde (OMS) para adultos, assinale a alternativa correta:","IMC entre 18,5 e 24,9 kg/m² indica sobrepeso","IMC entre 25,0 e 29,9 kg/m² indica obesidade grau I","IMC entre 30,0 e 34,9 kg/m² indica obesidade grau I","IMC acima de 35,0 kg/m² indica obesidade mórbida","IMC abaixo de 20,0 kg/m² indica desnutrição grave",c,"Segundo a classificação da OMS para adultos: IMC < 18,5 = baixo peso; 18,5-24,9 = eutrofia; 25,0-29,9 = sobrepeso (pré-obesidade); 30,0-34,9 = obesidade grau I; 35,0-39,9 = obesidade grau II; ≥ 40,0 = obesidade grau III (mórbida). Portanto, IMC entre 30,0 e 34,9 kg/m² corresponde a obesidade grau I."
authored-psi-001,HealthQuest,Psicologia,,MEDIUM,"Os Centros de Atenção Psicossocial (CAPS) são serviços estratégicos da Rede de Atenção Psicossocial (RAPS). Sobre o CAPS e seu papel na rede, assinale a alternativa correta:","O CAPS é a porta de entrada exclusiva para todos os atendimentos em saúde mental no SUS","O CAPS III funciona 24 horas, incluindo feriados e finais de semana, e oferece acolhimento noturno","O encaminhamento para o CAPS exige obrigatoriamente uma consulta prévia com psiquiatra","Crianças e adolescentes são atendidos exclusivamente no CAPS I","O CAPS substitui integralmente a internação psiquiátrica, que foi abolida no Brasil",b,"O CAPS III é a modalidade que funciona 24 horas, todos os dias, e oferece acolhimento noturno (até 14 dias). O CAPS não é porta exclusiva — a Atenção Básica também acolhe demandas de saúde mental. Crianças e adolescentes são atendidos no CAPSi (infanto-juvenil). A internação não foi abolida, mas deve ser o último recurso e preferencialmente de curta duração."
authored-psi-002,HealthQuest,Psicologia,,MEDIUM,"No contexto da escuta clínica em psicologia, o sigilo profissional é um princípio ético fundamental. Sobre os limites do sigilo, conforme o Código de Ética Profissional do Psicólogo (Resolução CFP 010/2005), é correto afirmar que:","O sigilo é absoluto e não pode ser quebrado em nenhuma circunstância","O psicólogo pode compartilhar informações com familiares sempre que julgar necessário","A quebra de sigilo é permitida quando há risco iminente à vida do paciente ou de terceiros","O sigilo se encerra automaticamente após o término do atendimento","Informações podem ser compartilhadas com outros profissionais de saúde sem consentimento do paciente",c,"O Código de Ética do Psicólogo (Art. 9º e 10º) estabelece que o sigilo pode ser quebrado em situações excepcionais, como risco iminente à vida do paciente ou de terceiros, ou quando determinado por ordem judicial. O sigilo persiste após o término do atendimento e o compartilhamento com outros profissionais requer consentimento, salvo exceções previstas."
authored-sc-001,HealthQuest,Saúde Coletiva,,MEDIUM,"O Sistema Único de Saúde (SUS) foi instituído pela Constituição Federal de 1988 e regulamentado pelas Leis 8.080/90 e 8.142/90. Sobre seus princípios doutrinários, assinale a alternativa que os apresenta corretamente:","Universalidade, equidade e descentralização","Integralidade, hierarquização e participação popular","Universalidade, integralidade e equidade","Equidade, regionalização e complementaridade do setor privado","Descentralização, universalidade e controle social",c,"Os três princípios doutrinários (ou ideológicos) do SUS são: Universalidade (acesso garantido a todas as pessoas), Integralidade (atendimento completo — promoção, prevenção, cura e reabilitação) e Equidade (tratar desigualmente os desiguais, priorizando quem mais necessita). Descentralização, regionalização e participação popular são princípios organizativos."
```

- [ ] **Step 3: Commit**

```bash
git add packages/db/prisma/seed/sources/
git commit -m "feat(db): add 15 authored pt-BR questions and sources README"
```

---

### Task 8: Write Integration Tests (TDD — Tests First)

**Files:**
- Create: `packages/db/__tests__/seed.test.ts`

- [ ] **Step 1: Create `packages/db/__tests__/seed.test.ts`**

```ts
import { describe, it, expect, beforeAll, afterEach } from "vitest";
import { writeFileSync, mkdirSync, rmSync } from "node:fs";
import { join } from "node:path";
import { tmpdir } from "node:os";
import { PrismaPg } from "@prisma/adapter-pg";
import { PrismaClient } from "../generated/prisma/client.js";
import { seed } from "../prisma/seed.js";

function createTestClient(): PrismaClient {
  const adapter = new PrismaPg({
    connectionString: process.env.DATABASE_URL ?? "",
  });
  return new PrismaClient({ adapter });
}

describe("seed", () => {
  let prisma: PrismaClient;

  beforeAll(() => {
    prisma = createTestClient();
  });

  afterEach(async () => {
    await prisma.answerHistory.deleteMany({
      where: { question: { externalId: { startsWith: "__test_" } } },
    });
    await prisma.alternative.deleteMany({
      where: { question: { externalId: { startsWith: "__test_" } } },
    });
    await prisma.question.deleteMany({
      where: { externalId: { startsWith: "__test_" } },
    });
  });

  it("seeds taxonomies and questions from CSV", async () => {
    const tmpDir = join(tmpdir(), `seed-test-${Date.now()}`);
    mkdirSync(tmpDir, { recursive: true });

    writeFileSync(
      join(tmpDir, "test.csv"),
      `externalId,institution,subject,year,difficulty,statement,a,b,c,d,e,correct,explanation
__test_q001,HealthQuest,Enfermagem,,MEDIUM,"Qual é a primeira etapa do processo de enfermagem?",Diagnóstico,Coleta de dados,Planejamento,Implementação,Avaliação,b,"A coleta de dados é a primeira etapa do processo de enfermagem segundo a SAE."
__test_q002,HealthQuest,Nutrição,,EASY,"O IMC é calculado dividindo peso pela altura ao quadrado. Qual a faixa de eutrofia?",< 18.5,18.5 a 24.9,25 a 29.9,30 a 34.9,> 40,b,"Segundo a OMS o IMC entre 18.5 e 24.9 kg/m² indica eutrofia em adultos."`,
    );

    const result = await seed({ sourcesDir: tmpDir });

    expect(result.subjects).toBe(5);
    expect(result.institutions).toBe(6);
    expect(result.questions).toBe(2);

    const questions = await prisma.question.findMany({
      where: { externalId: { startsWith: "__test_" } },
      include: { alternatives: { orderBy: { position: "asc" } } },
    });

    expect(questions).toHaveLength(2);
    for (const q of questions) {
      expect(q.alternatives).toHaveLength(5);
      const correctAlts = q.alternatives.filter((a) => a.isCorrect);
      expect(correctAlts).toHaveLength(1);
    }

    rmSync(tmpDir, { recursive: true, force: true });
  });

  it("is idempotent — re-running does not duplicate rows", async () => {
    const tmpDir = join(tmpdir(), `seed-test-idem-${Date.now()}`);
    mkdirSync(tmpDir, { recursive: true });

    const csv = `externalId,institution,subject,year,difficulty,statement,a,b,c,d,e,correct,explanation
__test_idem001,HealthQuest,Enfermagem,,MEDIUM,"Questão teste de idempotência para verificar que não duplica.",Alt A,Alt B,Alt C,Alt D,Alt E,a,"Explicação da questão teste de idempotência para o seed runner."`;

    writeFileSync(join(tmpDir, "test.csv"), csv);

    await seed({ sourcesDir: tmpDir });
    await seed({ sourcesDir: tmpDir });

    const questions = await prisma.question.findMany({
      where: { externalId: "__test_idem001" },
    });
    expect(questions).toHaveLength(1);

    rmSync(tmpDir, { recursive: true, force: true });
  });

  it("preserves Question.id and Alternative.id across re-runs", async () => {
    const tmpDir = join(tmpdir(), `seed-test-ids-${Date.now()}`);
    mkdirSync(tmpDir, { recursive: true });

    const csv = `externalId,institution,subject,year,difficulty,statement,a,b,c,d,e,correct,explanation
__test_stable001,HealthQuest,Enfermagem,,MEDIUM,"Questão para testar estabilidade dos IDs após re-run do seed.",A1,B1,C1,D1,E1,c,"Explicação da questão de estabilidade de IDs no seed runner."`;

    writeFileSync(join(tmpDir, "test.csv"), csv);

    await seed({ sourcesDir: tmpDir });

    const firstRun = await prisma.question.findUnique({
      where: { externalId: "__test_stable001" },
      include: { alternatives: { orderBy: { position: "asc" } } },
    });

    await seed({ sourcesDir: tmpDir });

    const secondRun = await prisma.question.findUnique({
      where: { externalId: "__test_stable001" },
      include: { alternatives: { orderBy: { position: "asc" } } },
    });

    expect(secondRun!.id).toBe(firstRun!.id);
    expect(secondRun!.alternatives[0].id).toBe(firstRun!.alternatives[0].id);
    expect(secondRun!.alternatives[4].id).toBe(firstRun!.alternatives[4].id);

    rmSync(tmpDir, { recursive: true, force: true });
  });

  it("preserves AnswerHistory across re-runs", async () => {
    const tmpDir = join(tmpdir(), `seed-test-history-${Date.now()}`);
    mkdirSync(tmpDir, { recursive: true });

    const csv = `externalId,institution,subject,year,difficulty,statement,a,b,c,d,e,correct,explanation
__test_history001,HealthQuest,Enfermagem,,MEDIUM,"Questão para testar preservação do AnswerHistory no seed.",HA,HB,HC,HD,HE,a,"Explicação para teste de preservação de AnswerHistory após re-run."`;

    writeFileSync(join(tmpDir, "test.csv"), csv);
    await seed({ sourcesDir: tmpDir });

    const question = await prisma.question.findUnique({
      where: { externalId: "__test_history001" },
      include: { alternatives: { orderBy: { position: "asc" } } },
    });

    const testUser = await prisma.user.create({
      data: {
        name: "__test_user",
        email: "__test_seed_history@test.com",
        password: "hashed",
      },
    });

    const history = await prisma.answerHistory.create({
      data: {
        userId: testUser.id,
        questionId: question!.id,
        alternativeId: question!.alternatives[0].id,
        isCorrect: true,
        responseTime: 5000,
      },
    });

    await seed({ sourcesDir: tmpDir });

    const preserved = await prisma.answerHistory.findUnique({
      where: { id: history.id },
    });
    expect(preserved).not.toBeNull();
    expect(preserved!.alternativeId).toBe(question!.alternatives[0].id);

    await prisma.answerHistory.delete({ where: { id: history.id } });
    await prisma.user.delete({ where: { id: testUser.id } });
    rmSync(tmpDir, { recursive: true, force: true });
  });

  it("throws on invalid CSV row with file and line info", async () => {
    const tmpDir = join(tmpdir(), `seed-test-bad-${Date.now()}`);
    mkdirSync(tmpDir, { recursive: true });

    writeFileSync(
      join(tmpDir, "bad.csv"),
      `externalId,institution,subject,year,difficulty,statement,a,b,c,d,e,correct,explanation
__test_bad001,HealthQuest,Enfermagem,,MEDIUM,"Short",A,B,C,D,E,z,"Explanation that is long enough to pass validation."`,
    );

    await expect(seed({ sourcesDir: tmpDir })).rejects.toThrow(/bad\.csv:2/);

    const questions = await prisma.question.findMany({
      where: { externalId: "__test_bad001" },
    });
    expect(questions).toHaveLength(0);

    rmSync(tmpDir, { recursive: true, force: true });
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run:
```bash
cd packages/db && pnpm test
```

Expected: `seed.test.ts` tests FAIL (cannot resolve `../prisma/seed.js` because seed.ts doesn't exist yet, or related import errors). The existing `client.test.ts` still passes.

- [ ] **Step 3: Commit failing tests**

```bash
git add packages/db/__tests__/seed.test.ts
git commit -m "test(db): add integration tests for seed pipeline (red phase)"
```

---

### Task 9: Make Tests Pass — Implement All Seed Modules

This task creates all the source files from Tasks 3–6 (if not done yet as standalone commits, create them now). After all modules exist, run the tests.

**Files:**
- Verify exist: `packages/db/prisma/seed/taxonomies.ts`
- Verify exist: `packages/db/prisma/seed/csv-row.schema.ts`
- Verify exist: `packages/db/prisma/seed/ingest.ts`
- Verify exist: `packages/db/prisma/seed.ts`
- Verify exist: `packages/db/prisma/seed/sources/healthquest-authored.csv`

- [ ] **Step 1: Ensure Docker Postgres is running**

Run:
```bash
docker compose up -d
```

Expected: postgres container running on port 5432.

- [ ] **Step 2: Run the full test suite**

Run:
```bash
cd packages/db && pnpm test
```

Expected: ALL tests pass — both `client.test.ts` (2 tests) and `seed.test.ts` (5 tests).

- [ ] **Step 3: If tests fail, debug and fix**

Common issues:
- Import path issues: ensure `.js` extensions on relative imports (ESM)
- `csv-parse/sync` import: use `import { parse } from "csv-parse/sync"`
- Prisma client not regenerated after schema change: run `pnpm exec prisma generate`
- Database not synced: run `pnpm exec prisma db push`

- [ ] **Step 4: Commit green tests**

```bash
git add -A packages/db/
git commit -m "feat(db): implement seed pipeline — all integration tests green"
```

---

### Task 10: Full Verification

- [ ] **Step 1: Run seed on fresh database**

Run:
```bash
cd packages/db && pnpm seed
```

Expected output includes: `Seed complete: { subjects: 5, institutions: 6, questions: 15 }`

- [ ] **Step 2: Run seed a second time (idempotency)**

Run:
```bash
cd packages/db && pnpm seed
```

Expected: same output, no errors, no duplicates.

- [ ] **Step 3: Verify via Prisma Studio (optional)**

Run:
```bash
cd packages/db && pnpm studio
```

Spot-check: one Enfermagem question has 5 alternatives with exactly one `isCorrect=true`.

- [ ] **Step 4: Run apps/web tests to confirm no regression**

Run:
```bash
pnpm --filter @healthquest/web test
```

Expected: all existing auth/db tests pass.

- [ ] **Step 5: Run full workspace build**

Run:
```bash
pnpm --filter @healthquest/db generate && pnpm --filter @healthquest/web build
```

Expected: clean build, no type errors.

- [ ] **Step 6: Final commit if any fixes were needed**

If any adjustments were made during verification, commit them:
```bash
git add -A
git commit -m "fix(db): address verification issues in seed pipeline"
```

---

## Execution Notes

- **Task ordering:** Tasks 1–7 create the source files. Task 8 writes tests (red phase). Task 9 runs them (green phase). Task 10 is end-to-end verification.
- **TDD note:** The spec mandates TDD. In strict TDD you write tests before implementation. However, the tests import the seed modules, so at minimum the module files must exist (even as stubs) before tests can compile. The practical approach: create all source files (Tasks 1–7), then write tests (Task 8), then run tests to see them pass (Task 9). If building iteratively, you can write stubs first and fill in implementation after the test file exists.
- **Database:** Tests and seed both need Docker Postgres running. The `DATABASE_URL` should be `postgresql://healthquest:healthquest@localhost:5432/healthquest_dev`.
- **ESM:** This package is `"type": "module"`. All relative imports need `.js` extensions. `tsx` handles TypeScript execution transparently.
